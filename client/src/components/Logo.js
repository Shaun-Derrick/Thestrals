import "../stylesheets/index.css"
import React from "react"

const Logo = () => {
  return (
    <div className="logoContainer">
      <i className={`icon-left huge react icon`} />
      <p className="logoText">FUSION</p>
      <i className={`icon-right huge react icon`} />
    </div>
  )
}

export default Logo
